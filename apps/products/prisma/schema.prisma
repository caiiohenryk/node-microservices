datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Product {
  id            Int        @id @default(autoincrement())
  name          String
  quantity      Int
  description   String?
  price         Float
}
