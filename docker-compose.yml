version: '3.8'

services:
  # Service Registry
  consul:
    image: consul:latest
    container_name: consul
    ports:
      - "8500:8500" # UI e HTTP API
      - "8600:8600/udp" # DNS Interface
    command: "agent -server -ui -node=server-1 -bootstrap-expect=1 -client=0.0.0.0"